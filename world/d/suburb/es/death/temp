// Room: /d/death/road3.c

inherit ROOM;

void create()
{
	set("short", "隔汉衫Y");
	set("long", @LONG

..... 临⊿稱 ....

LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "south" : "/d/death/road2",
]));

	setup();
	replace_program(ROOM);
}
